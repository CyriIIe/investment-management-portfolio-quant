[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "investment-management-portfolio-quant"
version = "0.1.0"
description = "Quantitative bond portfolio engine for Investment Management CORE"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
